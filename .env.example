# Copy to `.env` or `.env.local` in this folder and paste your key (never commit the real file).
# Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional: Imagen model id for asset generation scripts (generic products, hero, car still, …)
# GEMINI_IMAGE_MODEL=imagen-4.0-generate-001
